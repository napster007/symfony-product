{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.size) }}
    {{ form_row(form.description) }}
    {{ form_row(form.save,{ 'attr': {'class': 'btn btn-primary'}, 'label': 'Save Product' }) }}
{{ form_end(form) }}
